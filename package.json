{
  "name": "herbivory-counter",
  "version": "0.1.0",
  "description": "Leaf herbivory measurement tool for researchers",
  "main": "dist/main/index.js",
  "scripts": {
    "rebuild:electron": "electron-rebuild -f -w better-sqlite3",
    "rebuild:node": "npm rebuild better-sqlite3",
    "dev": "npm run rebuild:electron && env -u ELECTRON_RUN_AS_NODE electron-vite dev",
    "build": "electron-vite build",
    "preview": "npm run rebuild:electron && env -u ELECTRON_RUN_AS_NODE electron-vite preview",
    "test": "npm run rebuild:node && jest",
    "test:e2e": "npm run rebuild:electron && env -u ELECTRON_RUN_AS_NODE playwright test"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.2",
    "@playwright/test": "^1.40.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "electron-playwright-helpers": "^2.1.0",
    "electron-vite": "^2.0.0",
    "jest": "^29.7.0",
    "playwright": "^1.40.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^9.2.2"
  }
}
